import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const tempUserSlice = createSlice({
    name: "tempUser",
    initialState,
    reducers: {},
    extraReducers: (bulder) => {},
});
