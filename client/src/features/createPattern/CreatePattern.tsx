import { FormCreatePattern } from "../../entities";
import { Container } from "../../shared/ui";

const CreatePattern = () => {
    return (
        <Container>
            <FormCreatePattern />
        </Container>
    );
};

export default CreatePattern;
