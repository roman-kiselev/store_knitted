import { AddOneImg } from "../../entities";
import { Col, Container } from "../../shared/ui";

const CreatePattern = () => {
    return (
        <Container>
            <Col m={5} contentWidth="30%">
                <AddOneImg />
            </Col>
            <Col>dsggd</Col>
        </Container>
    );
};

export default CreatePattern;
